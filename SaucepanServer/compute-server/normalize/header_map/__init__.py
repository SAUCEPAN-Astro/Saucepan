"""
Header mapping: heterogeneous source headers -> Saucepan canonical SP_* schema.

Live path is the synonym vocabulary (``synonyms.yaml`` + ``vocab.py``):
concept -> ordered list of source keywords, first match wins, with
physical-domain validators. See ``docs/design/NORMALIZE_HEADER_MAP.md``.
"""
